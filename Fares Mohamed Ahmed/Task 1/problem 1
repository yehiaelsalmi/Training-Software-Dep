#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n;
    scanf("%d",&n);
    int a[n+1];
    int sum=0;
    
    while(n>0)
    {
        scanf("%d",&a[n]);
        sum+=a[n];
        n--;
        
        
    }
    printf("%d",sum);

    
}
